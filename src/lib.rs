#![no_std]


enum Color {
    Red,
    Green,
    Blue,
}

fn read_color(s0 : InputPint, s1 , s2, s3, out : Hertz) -> Color {

}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
